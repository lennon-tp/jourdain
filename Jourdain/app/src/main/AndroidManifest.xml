<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jourdain">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jourdain">

        <!-- Splash Screen -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Jourdain.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity android:name=".ui.home.MainActivity" />

        <!-- PDF Viewer -->
        <activity android:name=".ui.pdfviewer.PdfActivity" />

        <!-- Audio List -->
        <activity android:name=".ui.audio.AudioListActivity" />

        <!-- Audio Player -->
        <activity android:name=".ui.audio.AudioPlayerActivity" />

    </application>

</manifest>
